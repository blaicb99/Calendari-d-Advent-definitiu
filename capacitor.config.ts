import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elteunom.calendariadvent',
  appName: 'calendari-advent',
  webDir: 'dist'
};

export default config;
